{
    "name": "node",
    "version": "1.0.0",
    "description": "Boilerplate Node Lambda functions",
    "main": "index.js",
    "scripts": {
        "lint": "eslint --color .",
        "mocha": "mocha --recursive test -R mocha-junit-reporter --reporter-options mochaFile=test-results/junit.xml",
        "nyc": "nyc --reporter=cobertura npm run mocha",
        "coverage": "nyc report && nyc report --reporter=text-summary && nyc report --reporter=html",
        "test": "npm run lint && npm run nyc && npm run coverage"
    },
    "keywords": [
        "lambda"
    ],
    "author": "Miroslaw Majka",
    "license": "ISC",
    "devDependencies": {
        "chai": "^4.2.0",
        "eslint": "^7.1.0",
        "mocha": "^7.2.0",
        "mocha-junit-reporter": "^1.23.3",
        "nyc": "^15.1.0"
    }
}
